[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -ldflags=\"-w -s\" -o /app/bin/worker ."

[deploy]
startCommand = "/app/bin/worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
