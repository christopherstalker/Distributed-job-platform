[build]
builder = "NIXPACKS"
buildCommand = "go mod download && go build -ldflags=\"-w -s\" -o /app/bin/api ."

[deploy]
startCommand = "/app/bin/api"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
