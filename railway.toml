[build]
builder = "NIXPACKS"
buildCommand = "bash ./scripts/nixpacks-build.sh"

[deploy]
startCommand = "bash ./scripts/nixpacks-start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
